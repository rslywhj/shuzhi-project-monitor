# Comma-separated accounts that receive the system administrator role.
APP_ADMIN_EMAILS=admin@example.com
APP_SESSION_SECRET=replace-with-at-least-32-random-characters

# Optional. Keep false in production; set true only for a disposable demo database.
SEED_DEMO_DATA=false
